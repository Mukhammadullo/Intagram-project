import { createSlice } from "@reduxjs/toolkit";

let natification = createSlice({
  name: "Natification",
  initialState: {},
  reducers: {},
});
export let {} = natification.actions;
export default natification.reducer;
